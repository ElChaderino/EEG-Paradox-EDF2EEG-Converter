# GUI Requirements for EEG Paradox EDF Converter
# These are the minimum requirements for the GUI to work

# Core GUI framework
PyQt5>=5.15.0

# Data processing (required by the converter)
mne>=1.0.0
numpy>=1.20.0

# Optional but recommended for better performance
scipy>=1.7.0

# Note: The GUI will automatically check if the converter module is available
# and will show appropriate error messages if dependencies are missing
